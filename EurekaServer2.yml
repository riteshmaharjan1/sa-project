server:
  port: 8762
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://localhost:8762/eureka
  server:
    wait-time-in-ms-when-sync-empty: 0
