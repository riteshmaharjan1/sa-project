server:
  port: 8091

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  data:
    mongodb:
      auto-index-creation: true
      host: localhost
      port: 27017
      database: ProductDB
  application:
    name: ProductService